syntax = "proto3";

package network.forta;

option go_package = "./;protocol";

message Error {
  string message = 1;
}

enum ResponseStatus {
  UNKNOWN = 0;
  ERROR = 1;
  SUCCESS = 2;
}

message Response {
  ResponseStatus status = 1;
  repeated Error errors = 2;
}
